// 3rd party imports
// import * as React from 'react'

// My imports

export const useRegisterController = () => {
  const submit = async (values: any) => {
    console.log(values)
  }

  return {
    submit,
  }
}

// STYLING
